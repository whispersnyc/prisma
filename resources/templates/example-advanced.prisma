# Advanced Prisma Template Example
# Demonstrates regex matching and color component variables

@target HOME\.config\custom-app\theme.conf

# Match and replace any line containing "primary-color"
@match ".*primary-color.*"
primary-color = rgb({color1.r}, {color1.g}, {color1.b})

# Match and replace any line containing "background"
@match ".*background.*"
background = {background}

# Match and replace lines with "accent"
@match ".*accent.*"
accent-hue = {color4.h}
accent-saturation = {color4.s}
accent-lightness = {color4.l}

# Replace specific line range for theme definition
@lines 50-60
[theme]
name = "Pywal Dynamic Theme"
version = "1.0"

# Colors
bg = {background}
fg = {foreground}
accent = {color4}
highlight = {color2}

# RGB Components for custom processing
red = {color1.r}

# Append metadata at the end
@append

# Auto-generated by Prisma
# Color scheme based on current wallpaper
